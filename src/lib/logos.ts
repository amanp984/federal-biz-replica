import fullLogo from "@/assets/federal-bank-full.asset.json";
import horizontalLogo from "@/assets/federal-bank-horizontal.asset.json";

export const FEDERAL_LOGO_FULL = (fullLogo as { url: string }).url;
export const FEDERAL_LOGO_HORIZONTAL = (horizontalLogo as { url: string }).url;